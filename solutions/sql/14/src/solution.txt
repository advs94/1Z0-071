1. CREATE VIEW employees_vu AS
     SELECT employee_id,
            last_name AS employee,
            department_id
       FROM employees;
2. DESC employees_vu;
   SELECT *
     FROM employees_vu;
3. SELECT employee,
          department_id
     FROM employees_vu;
4. CREATE VIEW dept80 AS
     SELECT employee_id AS empno,
            last_name AS employee,
            department_id AS deptno
       FROM employees
      WHERE department_id = 80
      WITH CHECK OPTION;
5. DESC dept80;
   SELECT *
     FROM dept80;
6. UPDATE dept80
      SET deptno = 50
    WHERE employee = 'Abel';
7. SELECT view_name,
          text,
     FROM user_views;
8. DROP VIEW dept50;
   DROP VIEW dept80;
   DROP VIEW employees_vu;
